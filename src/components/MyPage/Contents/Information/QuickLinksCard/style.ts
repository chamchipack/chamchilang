import { StyleSheet } from 'react-native';

const CARD = '#1C1C1E';
const BORDER = '#2B2B2E';

export const styles = StyleSheet.create({
  card: {
    backgroundColor: CARD,
    borderWidth: 1,
    borderColor: BORDER,
    borderRadius: 12,
    padding: 12,
    gap: 10,
  },
});
